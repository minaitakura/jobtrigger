<div class="container">
  <div class="container-inner inner-default clearfix">
    <% jobs.each do |job| %>
      <div class="job-box">
        <div class="detail">
          <p class="title">
            <% #TODO 商品タイトル %>
          </p>
        </div>
        <div class="action clearfix">
          <%= link_to "" , carrerships_path(title: , type: "Hope") ,
                      :class => "hope-#{item.get('ASIN')} btn-hope btn btn-danger btn-sm want",
                      :remote => true,
                      :method => :post %>

        </div>
      </div>
    <% end %>
  </div>
</div>